<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Birthday Envelope</title>

<style>
body{
    margin:0;
    height:100vh;
    display:flex;
    justify-content:center;
    align-items:center;
    background:#f5f6f0;
    font-family:Arial, sans-serif;
}

/* Envelope */
.envelope{
    position:relative;
    width:340px;
    height:220px;
    background:#e89cad;
    border-radius:20px;
    box-shadow:0 20px 35px rgba(0,0,0,0.2);
    overflow:hidden;
    display:flex;
    justify-content:center;
    align-items:center;
}

/* Flap */
.flap{
    position:absolute;
    top:0;
    left:0;
    width:100%;
    height:100%;
    background:#f07c99;
    clip-path: polygon(0 0, 50% 55%, 100% 0);
    transform-origin:top;
    transition:0.6s;
}

/* Button in center */
.open-btn{
    position:absolute;
    z-index:2;
    padding:10px 20px;
    border:none;
    background:white;
    color:#f07c99;
    font-size:16px;
    border-radius:20px;
    cursor:pointer;
    font-weight:bold;
}

/* Letter */
.letter{
    position:absolute;
    bottom:-100%;
    width:100%;
    height:100%;
    background:white;
    border-radius:15px;
    display:flex;
    justify-content:center;
    align-items:center;
    text-align:center;
    padding:20px;
    box-sizing:border-box;
    transition:0.6s;
}

/* Open Animation */
.envelope.open .flap{
    transform:rotateX(180deg);
}

.envelope.open .letter{
    bottom:0;
}

.envelope.open .open-btn{
    display:none;
}
</style>
</head>

<body>

<div class="envelope" id="envelope">
    <div class="flap"></div>

    <button class="open-btn" onclick="openEnvelope()">Open 💌</button>

    <div class="letter">
        <div>
            <h2>🎂 Happy Birthday! 🎉</h2>
            <p>May your special day be filled with love, joy and beautiful memories!</p>
        </div>
    </div>
</div>

<script>
function openEnvelope(){
    document.getElementById("envelope").classList.add("open");
}
</script>

</body>
</html>
